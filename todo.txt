goals for tommorow:
fabrication menu

NETWORKING REWORK NOTES:

Network autoload contains methods for sending and syncing state using
a network of Networked_Objects that act as state syncers. 
Client/server calls these methods suck as "send_world_state".
Networked_Object node can be referenced by nearby related nodes if added to the objects
"related nodes" list.


Notes:
if you sync player views and selctions, just make a player node and sync thoes
